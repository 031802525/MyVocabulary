<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.desktop">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fun.LockScreenActivity"></activity>
        <activity android:name=".Fun.RemainActivity" />
        <activity android:name=".Fun.AddWordActivity" />
        <activity android:name=".Fun.WordBagActivity" />
        <activity android:name=".Fun.LinkGameActivity" />
        <activity android:name=".Register.ForgetPwdActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".Register.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>